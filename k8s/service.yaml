apiVersion: v1
kind: Service
metadata:
  name: teamcast-backend-service
  namespace: teamcast
  labels:
    app: teamcast-backend
spec:
  selector:
    app: teamcast-backend
  ports:
    - name: http
      port: 80
      targetPort: 4300
      protocol: TCP
  type: ClusterIP
